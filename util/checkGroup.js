// require app modules
const { executeQuery } = require("../config/db")

const checkGroup = (username, group) => {
    // select * from users where username = 'username' and groups like 'group'
    // if result.length > 0, return true
    // else return false
}

module.exports = checkGroup